class InitHandler {
    handleRequest(argument) {
        switch (argument) {
            case '--help':
                break
            case '--all':
                break
            case '--mk':
                break
            case '--cat':
                break
            default:
                break
        }
    }

    createFolders() {

    }

    createFiles() {

    }
}

module.exports = {InitHandler}
