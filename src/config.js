class ConfigHandler {
    handleRequest(argument, {option, value}) {
        switch (argument) {
            case '--help':
                break
            case '--show':
                break
            case '--reset':
                break
            case '--set':
                break
            default:
                break
        }
    }

    setConfig() {

    }

    showConfig() {

    }
}

module.exports = {ConfigHandler}
